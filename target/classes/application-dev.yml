spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupons?useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
    schema: classpath:import.sql
    data: classpath:data.sql
    initialize: false
  jpa:
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
  redis:
    host: localhost
    port: 6379
#    username: root
#    password: root
server:
  port: 7777

